/* Minimum- did this one by myself! I thought this problem was the easiest, because it 
only looked like a math problem */

function min(x, y) {
  if (x <= y)
    return x;
  else if (y <= x)
    return y;
}
console.log(min(3, 2));

